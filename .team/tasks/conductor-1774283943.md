---
conductor_id: conductor-1774283943
issue_id: 010
task_type: implementer
---

## Task: Create spawn-conductor.sh script

Create a shell script to deterministically spawn Conductor sessions, removing complex cmux/worktree logic from Manager.

### Deliverables

1. `.team/scripts/spawn-conductor.sh`
   - Input: <task-id> (e.g., "009")
   - Process: spawn new conductor with worktree, cmux pane, initial prompt
   - Output: CONDUCTOR_ID, SURFACE, TASK_ID to stdout
   - Handle Trust confirmation auto-approval
   
2. Update `skills/cmux-team/templates/manager.md` §2
   - Simplify: replace detailed cmux logic with single script call
   - Manager just runs: `bash .team/scripts/spawn-conductor.sh ${TASK_ID}`

### Requirements

- Deterministic (no AI involved in spawn process)
- Handle errors gracefully (exit 1 on failure)
- Auto-approve Trust confirmation
- Output structured format for Manager to parse

