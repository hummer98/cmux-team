{
  "name": "cmux-team",
  "version": "3.36.0",
  "description": "Multi-agent development orchestration with Claude Code + cmux. Spawn, monitor, and integrate parallel sub-agents visually in terminal panes.",
  "author": {
    "name": "hummer98",
    "url": "https://github.com/hummer98"
  },
  "homepage": "https://github.com/hummer98/cmux-team",
  "repository": "https://github.com/hummer98/cmux-team",
  "license": "MIT",
  "keywords": ["cmux", "multi-agent", "orchestration", "parallel", "team"],
  "skills": "./skills/",
  "commands": "./commands/",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -z \"$CMUX_SURFACE\" ] && [ -n \"$CMUX_SURFACE_ID\" ]; then REF=$(cmux identify | jq -r '.caller.surface_ref'); NUM=$(echo \"$REF\" | cut -d: -f2); cmux rename-tab --surface \"$REF\" \"[$NUM] Claude Code\"; fi"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); FILE=$(echo \"$INPUT\" | jq -r '.tool_input.file_path // empty'); if echo \"$FILE\" | grep -qE '\\.team/(team|task-state)\\.json$'; then echo 'team.json と task-state.json は daemon が管理するファイルです。直接編集しないでください。タスク操作は cmux-team create-task / update-task / close-task コマンドを使用してください。'; exit 2; fi"
          }
        ]
      }
    ]
  }
}
